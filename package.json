{
  "name": "geminiex",
  "version": "1.0.0",
  "description": "A Gemini Browser extention which make it easier to use gemini every where on the internet!",
  "main": "manifest.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@google/generative-ai": "^0.19.0",
    "dotenv": "^16.4.5"
  }
}
